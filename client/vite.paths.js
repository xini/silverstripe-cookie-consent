// ============================================
// ENTRY POINTS - Edit these lists as needed
// ============================================

// JavaScript entry points
// - Can be vanilla JS (IIFE) or ES modules (import/export)
// - Each entry becomes a separate bundle in dist/js/
export const JS_ENTRIES = {
    cookieconsent: 'src/js/cookieconsent.js',
};

// CSS/SCSS entry points
// - Can be .scss or .css files
// - Each entry becomes a separate bundle in dist/css/
export const CSS_ENTRIES = {
    cookieconsent: 'src/scss/cookieconsent.scss',
    cookieconsentcheckboxfield: 'src/scss/cookieconsentcheckboxfield.scss',
};
